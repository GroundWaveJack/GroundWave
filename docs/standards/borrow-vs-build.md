# Borrow versus build

Build only when a user-level semantic cannot be expressed through a mature standard or a thin profile. An RFC must describe the gap, evaluated alternatives, layer ownership, security cost, interop path, upstream discussion, maintenance capacity, migration, and removal criteria.

Groundwave should not build new cryptography, generic routing, codecs, databases, radio firmware, operating systems, or package managers merely to appear complete.
